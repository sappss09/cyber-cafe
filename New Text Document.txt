<?php

$conn = mysqli_connect("localhost", "root", "", "cybercafe");

if (!$conn) {
    die("Database connection failed");
}

// Add Customer
if (isset($_POST['add'])) {

    $name = $_POST['name'];
    $phone = $_POST['phone'];
    $computer = $_POST['computer'];
    $start_time = $_POST['start_time'];
    $duration = $_POST['duration'];
    $amount = $_POST['amount'];

    $sql = "INSERT INTO customers
            (name, phone, computer_no, start_time, duration, amount)
            VALUES
            ('$name', '$phone', '$computer', '$start_time', '$duration', '$amount')";

    mysqli_query($conn, $sql);
}

// Delete Customer Record
if (isset($_GET['delete'])) {

    $id = $_GET['delete'];

    mysqli_query($conn, "DELETE FROM customers WHERE id=$id");

    header("Location: index.php");
    exit();
}

// Display Records
$result = mysqli_query($conn, "SELECT * FROM customers");

?>

<!DOCTYPE html>
<html>

<head>

    <title>Cyber Café Management System</title>

    <style>

        body {
            font-family: Arial;
            background-color: #f2f2f2;
            margin: 30px;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        form {
            width: 400px;
            margin: auto;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
        }

        input {
            width: 95%;
            padding: 9px;
            margin: 7px 0;
        }

        button {
            padding: 10px 20px;
            background-color: green;
            color: white;
            border: none;
            cursor: pointer;
        }

        table {
            width: 95%;
            margin: 30px auto;
            border-collapse: collapse;
            background-color: white;
        }

        th, td {
            border: 1px solid black;
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #333;
            color: white;
        }

        .delete {
            color: red;
        }

    </style>

</head>

<body>

<h1>Cyber Café Management System</h1>

<form method="POST">

    <label>Customer Name:</label>
    <input type="text" name="name" required>

    <label>Phone Number:</label>
    <input type="text" name="phone" required>

    <label>Computer Number:</label>
    <input type="number" name="computer" required>

    <label>Starting Time:</label>
    <input type="text" name="start_time" placeholder="10:30 AM" required>

    <label>Duration (Hours):</label>
    <input type="number" name="duration" required>

    <label>Total Amount:</label>
    <input type="number" name="amount" required>

    <button type="submit" name="add">Add Customer</button>

</form>

<table>

    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Phone</th>
        <th>Computer No.</th>
        <th>Start Time</th>
        <th>Duration</th>
        <th>Amount</th>
        <th>Action</th>
    </tr>

    <?php while ($row = mysqli_fetch_assoc($result)) { ?>

    <tr>

        <td><?php echo $row['id']; ?></td>

        <td><?php echo $row['name']; ?></td>

        <td><?php echo $row['phone']; ?></td>

        <td><?php echo $row['computer_no']; ?></td>

        <td><?php echo $row['start_time']; ?></td>

        <td><?php echo $row['duration']; ?> hours</td>

        <td>Rs. <?php echo $row['amount']; ?></td>

        <td>
            <a class="delete"
               href="index.php?delete=<?php echo $row['id']; ?>"
               onclick="return confirm('Are you sure you want to delete this record?')">
               Delete
            </a>
        </td>

    </tr>

    <?php } ?>

</table>

</body>

</html>
